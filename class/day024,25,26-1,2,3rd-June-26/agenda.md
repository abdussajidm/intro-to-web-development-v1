# Agenda for 1st, 2nd, 3rd June, 2026

## CSS position: static, fixed, sticky, relative, absolute.

- top, bottom, left, right, inset properties, z-index

how `left` and `right` behave when both are applied with the same value on a `position: relative` element?

- `left` usually takes effect and `right` is ignored in normal left-to-right layouts.
- They do not add together, so the element is not moved twice.

## develop 'coffee masters' website using html and css.

- refer same design links in projects/design-links.md

## introduction to javascript:

comparison operators, (>, <, >=, <=, ==, ===, !=, !==), students to explore it.

- loops (for, while)
